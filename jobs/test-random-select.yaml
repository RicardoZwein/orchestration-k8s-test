job_name: test-random-select
type: db2-select
query: |
  SELECT 
    id,
    username,
    CURRENT_TIMESTAMP AS run_time
  FROM public.users
  ORDER BY RAND()
  FETCH FIRST 10 ROWS ONLY
output: test_random.csv
schedule: "*/1 * * * *"  # Every minute
notify: false